/*
 * PokemonDetailPage Messages
 *
 * This contains all the text for the PokemonDetailPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PokemonDetailPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PokemonDetailPage container!',
  },
});
