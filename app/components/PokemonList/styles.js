import styled from 'styled-components';

import { Link } from 'react-router-dom';

const PokemonListDiv = styled.div`
  padding: 30px;
`;

const LinkDetailPage = styled(Link)`
  color: #00d451 !important;
`;

export { PokemonListDiv, LinkDetailPage };
